import React from 'react';
//This section will not be used for now......
const Rank = ({ name, entries }) => {
  return (
    <div>
      <div className='white f3'>
        {`${name}`}
      </div>
      
    </div>
  );
}

export default Rank;